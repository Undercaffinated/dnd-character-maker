THE TO-DO LIST

SPECIFIC
- Polish up step 2b so that the value of Remaining Bonus Points decreases when players allocate bonus points.

ABSTRACT
- Work to improve the modularity of the codebase, especially functions.
- Consider breaking up files like make.js into individual files per function, then putting those functions into subdirectories of javascript.
    - Ensure before doing this that index.html can in fact call a whole directory to be loaded.
    OR
    - Find a better way to load a jillion files without having to make a new html line for each.

ACQUIRE KNOWLEDGE


FOR VERSION 2
- Rewrite the whole thing as a webapp in Rust.
